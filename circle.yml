machine:
  xcode:
    version: 9.0
  environment:
    UNITY_HOME: /Applications/Unity
    LC_ALL: en_US.UTF-8
    LANG: en_US.UTF-8
test:
  override:
    - rake package:download package:import config:all build:ios
dependencies:
  pre:
    - gem install bundler
    - bundle install
    - ./setup
    - bundle exec fastlane match development
  cache_directories:
    - ~/Library/Caches/Homebrew/
checkout:
  post:
    - git fetch --tags
deployment:
  prod:
    branch: [master]
    commands:
      - aws s3 cp teak-unity-cleanroom.ipa s3://teak-build-artifacts/unity-cleanroom/teak-unity-cleanroom.ipa --acl public-read
